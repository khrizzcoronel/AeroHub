[project]
name = "aerohub-kernel"
version = "0.1.0"
description = "Tipos de dominio comunes a todos los modulos: dinero, tiempo UTC, codigos IATA/ICAO"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aerohub_kernel"]
